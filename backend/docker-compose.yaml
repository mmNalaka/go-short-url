version: "3.8"

services:
  redis:
    image: redis:6.2-alpine
    restart: always
    ports:
      - 6379:6379
    volumes:
      - ./data/redis:/data
    environment:
      - REDIS_PASSWORD=redis

volumes:
  cache:
    driver: local

